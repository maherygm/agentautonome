# Copier vers .env et adapter si besoin :
#   cp .env.example .env
#
# Chargé automatiquement au démarrage (CLI / FastAPI) via python-dotenv.

# Modèle Ollama (doit être pullé localement)
OLLAMA_MODEL=qwen2.5:7b

# Endpoint chat Ollama
OLLAMA_URL=http://localhost:11434/api/chat
